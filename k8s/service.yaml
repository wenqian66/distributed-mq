apiVersion: v1
kind: Service
metadata:
  name: mq-service
  namespace: default
  labels:
    app: mq-service
spec:
  type: ClusterIP
  selector:
    app: mq-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
